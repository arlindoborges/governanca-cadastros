import { AreaPlaceholder } from "@/components/shared/AreaPlaceholder";

export default function GovernancaPage() {
  return (
    <AreaPlaceholder
      title="Governança"
      description="Configuração progressiva de perfis, categorias, atributos e regras."
    />
  );
}
