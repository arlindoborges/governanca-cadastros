import { AreaPlaceholder } from "@/components/shared/AreaPlaceholder";

export default function ImportacoesPage() {
  return (
    <AreaPlaceholder
      title="Importações"
      description="Envio de arquivos, mapeamento de colunas e acompanhamento da validação."
    />
  );
}
