import { AreaPlaceholder } from "@/components/shared/AreaPlaceholder";

export default function ResultadosPage() {
  return (
    <AreaPlaceholder
      title="Resultados"
      description="Indicadores do estado vigente e exportação sob demanda."
    />
  );
}
