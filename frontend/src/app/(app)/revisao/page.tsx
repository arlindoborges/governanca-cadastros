import { AreaPlaceholder } from "@/components/shared/AreaPlaceholder";

export default function RevisaoPage() {
  return (
    <AreaPlaceholder
      title="Revisão"
      description="Fila de exceções, evidências, bloqueadores e decisão humana."
    />
  );
}
